{
  "name": "@luckytoken/application-control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": { "./control-plane": { "types": "./dist/control-plane.d.ts", "import": "./dist/control-plane.js" } },
  "dependencies": {
    "@luckytoken/provider-contract": "0.1.0"
  },
  "scripts": { "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.build.json", "typecheck": "tsc -p tsconfig.json --noEmit" }
}
