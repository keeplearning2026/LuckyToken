/**
 * CommandCode Private provider identity constants.
 *
 * Split from model.ts so the model catalog can import them without a cycle.
 */

export const COMMANDCODE_BASE_URL = "https://api.commandcode.ai";
export const COMMANDCODE_PROVIDER_ID = "commandcode-private" as const;
export const COMMANDCODE_API_ID = "commandcode-private" as const;
