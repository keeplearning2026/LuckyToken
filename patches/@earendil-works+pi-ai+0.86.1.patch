diff --git a/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.d.ts
index c79e885..9ed7c6e 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.d.ts
@@ -56,8 +56,12 @@ export interface AnthropicOptions extends StreamOptions {
      * Default: omitted (Anthropic default behavior, currently equivalent to auto).
      */
     toolChoice?: "auto" | "any" | "none" | {
+        type: "auto" | "any";
+        disable_parallel_tool_use?: boolean;
+    } | {
         type: "tool";
         name: string;
+        disable_parallel_tool_use?: boolean;
     };
     /**
      * Pre-built Anthropic client instance. When provided, skips internal client
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.js b/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.js
index d462f30..2ef082e 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/anthropic-messages.js
@@ -659,13 +659,33 @@ function mapThinkingLevelToEffort(model, level) {
             return "high";
     }
 }
+function mapSimpleToolChoice(choice, parallelToolCalls) {
+    if (choice === "none")
+        return "none";
+    const type = choice === "required" ? "any" : choice?.type === "tool" ? "tool" : choice;
+    if (type === undefined)
+        return parallelToolCalls === false ? { type: "auto", disable_parallel_tool_use: true } : undefined;
+    if (type === "tool") {
+        return {
+            type: "tool",
+            name: choice.name,
+            ...(parallelToolCalls === false ? { disable_parallel_tool_use: true } : {}),
+        };
+    }
+    return parallelToolCalls === false
+        ? { type, disable_parallel_tool_use: true }
+        : type;
+}
 export const streamSimple = (model, context, options) => {
     assertRequestAuth(model.provider, options?.apiKey, options?.headers);
     const base = {
         ...buildBaseOptions(model, context, options, options?.apiKey),
-        toolChoice: options?.toolChoice,
+        toolChoice: mapSimpleToolChoice(options?.toolChoice, options?.parallelToolCalls),
     };
-    if (!options?.reasoning) {
+    if (options?.reasoning === undefined) {
+        return stream(model, context, base);
+    }
+    if (options.reasoning === "off") {
         return stream(model, context, {
             ...base,
             thinkingEnabled: false,
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/azure-openai-responses.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/azure-openai-responses.d.ts
index 4a0f3ae..32c3d66 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/azure-openai-responses.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/azure-openai-responses.d.ts
@@ -1,8 +1,9 @@
 import type { ResponseCreateParamsStreaming } from "openai/resources/responses/responses.js";
-import type { SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
+import type { ModelThinkingLevel, SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
 export interface AzureOpenAIResponsesOptions extends StreamOptions {
-    reasoningEffort?: "minimal" | "low" | "medium" | "high" | "xhigh" | "max";
+    reasoningEffort?: ModelThinkingLevel;
     toolChoice?: ResponseCreateParamsStreaming["tool_choice"];
+    parallelToolCalls?: boolean;
     reasoningSummary?: "auto" | "detailed" | "concise" | null;
     azureApiVersion?: string;
     azureResourceName?: string;
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/azure-openai-responses.js b/node_modules/@earendil-works/pi-ai/dist/api/azure-openai-responses.js
index ec1e206..7b31a4e 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/azure-openai-responses.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/azure-openai-responses.js
@@ -119,6 +119,11 @@ export const stream = (model, context, options) => {
     })();
     return stream;
 };
+function mapSimpleToolChoice(choice) {
+    return choice?.type === "tool"
+        ? { type: "function", name: choice.name }
+        : choice;
+}
 export const streamSimple = (model, context, options) => {
     const apiKey = options?.apiKey;
     if (!apiKey) {
@@ -126,10 +131,12 @@ export const streamSimple = (model, context, options) => {
     }
     const base = {
         ...buildBaseOptions(model, context, options, apiKey),
-        toolChoice: options?.toolChoice,
+        toolChoice: mapSimpleToolChoice(options?.toolChoice),
+        parallelToolCalls: options?.parallelToolCalls,
     };
-    const clampedReasoning = options?.reasoning ? clampThinkingLevel(model, options.reasoning) : undefined;
-    const reasoningEffort = clampedReasoning === "off" ? undefined : clampedReasoning;
+    const reasoningEffort = options?.reasoning === undefined || options.reasoning === "off"
+        ? options?.reasoning
+        : clampThinkingLevel(model, options.reasoning);
     return stream(model, context, {
         ...base,
         reasoningEffort,
@@ -221,7 +228,10 @@ function buildParams(model, context, options, deploymentName, grammarToolInputPr
         store: false,
     };
     if (options?.maxTokens) {
-        params.max_output_tokens = Math.max(options.maxTokens, OPENAI_RESPONSES_MIN_OUTPUT_TOKENS);
+        if (options.maxTokens < OPENAI_RESPONSES_MIN_OUTPUT_TOKENS) {
+            throw new Error(`Azure OpenAI Responses requires maxTokens >= ${OPENAI_RESPONSES_MIN_OUTPUT_TOKENS}`);
+        }
+        params.max_output_tokens = options.maxTokens;
     }
     if (options?.temperature !== undefined) {
         params.temperature = options?.temperature;
@@ -235,21 +245,21 @@ function buildParams(model, context, options, deploymentName, grammarToolInputPr
     if (options?.toolChoice !== undefined) {
         params.tool_choice = options.toolChoice;
     }
+    if (options?.parallelToolCalls !== undefined) {
+        params.parallel_tool_calls = options.parallelToolCalls;
+    }
     if (model.reasoning) {
-        if (options?.reasoningEffort || options?.reasoningSummary) {
-            const effort = options?.reasoningEffort
-                ? (model.thinkingLevelMap?.[options.reasoningEffort] ?? options.reasoningEffort)
+        if (options?.reasoningEffort !== undefined || options?.reasoningSummary) {
+            const effort = options?.reasoningEffort !== undefined
+                ? (model.thinkingLevelMap?.[options.reasoningEffort] ?? (options.reasoningEffort === "off" ? "none" : options.reasoningEffort))
                 : "medium";
-            params.reasoning = {
-                effort: effort,
-                summary: options?.reasoningSummary || "auto",
-            };
-            params.include = ["reasoning.encrypted_content"];
-        }
-        else if (model.thinkingLevelMap?.off !== null) {
-            params.reasoning = {
-                effort: (model.thinkingLevelMap?.off ?? "none"),
-            };
+            if (effort !== null) {
+                params.reasoning = {
+                    effort: effort,
+                    summary: options?.reasoningSummary || "auto",
+                };
+                params.include = ["reasoning.encrypted_content"];
+            }
         }
     }
     // Last so custom keys override the named request fields.
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/bedrock-converse-stream.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/bedrock-converse-stream.d.ts
index 5d44bf0..7b6bc30 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/bedrock-converse-stream.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/bedrock-converse-stream.d.ts
@@ -1,4 +1,4 @@
-import type { SimpleStreamOptions, StreamFunction, StreamOptions, ThinkingBudgets, ThinkingLevel } from "../types.ts";
+import type { ModelThinkingLevel, SimpleStreamOptions, StreamFunction, StreamOptions, ThinkingBudgets } from "../types.ts";
 export type BedrockThinkingDisplay = "summarized" | "omitted";
 export interface BedrockOptions extends StreamOptions {
     region?: string;
@@ -7,7 +7,7 @@ export interface BedrockOptions extends StreamOptions {
         type: "tool";
         name: string;
     };
-    reasoning?: ThinkingLevel;
+    reasoning?: ModelThinkingLevel;
     thinkingBudgets?: ThinkingBudgets;
     interleavedThinking?: boolean;
     /**
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/bedrock-converse-stream.js b/node_modules/@earendil-works/pi-ai/dist/api/bedrock-converse-stream.js
index 44e3f13..02be842 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/bedrock-converse-stream.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/bedrock-converse-stream.js
@@ -389,9 +389,9 @@ function addResponseHeadersMiddleware(client, onResponse, model, onObserved) {
 export const streamSimple = (model, context, options) => {
     const base = {
         ...buildBaseOptions(model, context, options, undefined),
-        toolChoice: options?.toolChoice,
+        toolChoice: options?.toolChoice === "required" ? "any" : options?.toolChoice,
     };
-    if (!options?.reasoning) {
+    if (options?.reasoning === undefined || options.reasoning === "off") {
         return stream(model, context, { ...base, reasoning: undefined });
     }
     if (isAnthropicClaudeModel(model)) {
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.d.ts
index 3d9a16d..5386b0f 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.d.ts
@@ -1,7 +1,10 @@
 import type { SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
 import type { GoogleApiThinkingLevel } from "./google-shared.ts";
 export interface GoogleOptions extends StreamOptions {
-    toolChoice?: "auto" | "none" | "any";
+    toolChoice?: "auto" | "none" | "any" | {
+        type: "tool";
+        name: string;
+    };
     thinking?: {
         enabled: boolean;
         budgetTokens?: number;
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.js b/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.js
index 1e9e28e..978fa9e 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/google-generative-ai.js
@@ -239,9 +239,12 @@ export const streamSimple = (model, context, options) => {
     }
     const base = {
         ...buildBaseOptions(model, context, options, apiKey),
-        toolChoice: options?.toolChoice,
+        toolChoice: options?.toolChoice === "required" ? "any" : options?.toolChoice,
     };
-    if (!options?.reasoning) {
+    if (options?.reasoning === undefined) {
+        return stream(model, context, base);
+    }
+    if (options.reasoning === "off") {
         return stream(model, context, { ...base, thinking: { enabled: false } });
     }
     const clampedReasoning = clampThinkingLevel(model, options.reasoning);
@@ -296,6 +299,9 @@ function buildParams(model, context, options = {}) {
     const functionCallingMode = currentTools.length > 0
         ? resolveGoogleFunctionCallingMode(currentTools, options.toolChoice, supportsStrictMode)
         : undefined;
+    const allowedFunctionNames = options.toolChoice?.type === "tool"
+        ? [options.toolChoice.name]
+        : undefined;
     const systemInstruction = initialSystemMessage ? getSystemMessageText(initialSystemMessage) : "";
     const config = {
         ...(Object.keys(generationConfig).length > 0 && generationConfig),
@@ -304,7 +310,10 @@ function buildParams(model, context, options = {}) {
             tools: convertTools(currentTools, false, supportsStrictMode),
         }),
         ...(functionCallingMode !== undefined && {
-            toolConfig: { functionCallingConfig: { mode: functionCallingMode } },
+            toolConfig: { functionCallingConfig: {
+                    mode: functionCallingMode,
+                    ...(allowedFunctionNames === undefined ? {} : { allowedFunctionNames }),
+                } },
         }),
     };
     if (options.thinking?.enabled && model.reasoning) {
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/google-shared.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/google-shared.d.ts
index fd2322e..2f13b77 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/google-shared.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/google-shared.d.ts
@@ -70,7 +70,10 @@ export declare function convertTools(tools: Tool[], useParameters?: boolean, sup
 export declare function supportsGoogleStrictToolSampling(modelId: string): boolean;
 /** Map tool choice string to Gemini FunctionCallingConfigMode. */
 export declare function mapToolChoice(choice: string): FunctionCallingConfigMode;
-export declare function resolveGoogleFunctionCallingMode(tools: Tool[], toolChoice: string | undefined, supportsStrictMode: boolean): FunctionCallingConfigMode | undefined;
+export declare function resolveGoogleFunctionCallingMode(tools: Tool[], toolChoice: string | {
+    type: "tool";
+    name: string;
+} | undefined, supportsStrictMode: boolean): FunctionCallingConfigMode | undefined;
 /**
  * Map Gemini FinishReason to our StopReason.
  */
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/google-shared.js b/node_modules/@earendil-works/pi-ai/dist/api/google-shared.js
index 478c2c3..54b346f 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/google-shared.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/google-shared.js
@@ -369,6 +369,9 @@ export function mapToolChoice(choice) {
 }
 export function resolveGoogleFunctionCallingMode(tools, toolChoice, supportsStrictMode) {
     const useStrictMode = tools.some((tool) => resolveJsonSchemaStrictSampling(tool, supportsStrictMode) === true);
+    if (toolChoice?.type === "tool") {
+        return FunctionCallingConfigMode.ANY;
+    }
     if (toolChoice === "none" || toolChoice === "any") {
         return mapToolChoice(toolChoice);
     }
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.d.ts
index 9ef58ca..a2e6f14 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.d.ts
@@ -1,7 +1,10 @@
 import type { SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
 import type { GoogleApiThinkingLevel } from "./google-shared.ts";
 export interface GoogleVertexOptions extends StreamOptions {
-    toolChoice?: "auto" | "none" | "any";
+    toolChoice?: "auto" | "none" | "any" | {
+        type: "tool";
+        name: string;
+    };
     thinking?: {
         enabled: boolean;
         budgetTokens?: number;
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.js b/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.js
index 85ca3a5..903890e 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/google-vertex.js
@@ -237,9 +237,12 @@ export const stream = (model, context, options) => {
 export const streamSimple = (model, context, options) => {
     const base = {
         ...buildBaseOptions(model, context, options, undefined),
-        toolChoice: options?.toolChoice,
+        toolChoice: options?.toolChoice === "required" ? "any" : options?.toolChoice,
     };
-    if (!options?.reasoning) {
+    if (options?.reasoning === undefined) {
+        return stream(model, context, base);
+    }
+    if (options.reasoning === "off") {
         return stream(model, context, {
             ...base,
             thinking: { enabled: false },
@@ -366,6 +369,9 @@ function buildParams(model, context, options = {}) {
     const functionCallingMode = currentTools.length > 0
         ? resolveGoogleFunctionCallingMode(currentTools, options.toolChoice, supportsStrictMode)
         : undefined;
+    const allowedFunctionNames = options.toolChoice?.type === "tool"
+        ? [options.toolChoice.name]
+        : undefined;
     const systemInstruction = initialSystemMessage ? getSystemMessageText(initialSystemMessage) : "";
     const config = {
         ...(Object.keys(generationConfig).length > 0 && generationConfig),
@@ -374,7 +380,10 @@ function buildParams(model, context, options = {}) {
             tools: convertTools(currentTools, false, supportsStrictMode),
         }),
         ...(functionCallingMode !== undefined && {
-            toolConfig: { functionCallingConfig: { mode: functionCallingMode } },
+            toolConfig: { functionCallingConfig: {
+                    mode: functionCallingMode,
+                    ...(allowedFunctionNames === undefined ? {} : { allowedFunctionNames }),
+                } },
         }),
     };
     if (options.thinking?.enabled && model.reasoning) {
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/mistral-conversations.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/mistral-conversations.d.ts
index ed73ef2..7c322d7 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/mistral-conversations.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/mistral-conversations.d.ts
@@ -10,6 +10,7 @@ export interface MistralOptions extends StreamOptions {
             name: string;
         };
     };
+    parallelToolCalls?: boolean;
     promptMode?: "reasoning";
     reasoningEffort?: MistralReasoningEffort;
 }
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/mistral-conversations.js b/node_modules/@earendil-works/pi-ai/dist/api/mistral-conversations.js
index 3106c5d..531e8ef 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/mistral-conversations.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/mistral-conversations.js
@@ -70,15 +70,21 @@ export const streamSimple = (model, context, options) => {
     }
     const base = {
         ...buildBaseOptions(model, context, options, apiKey),
-        toolChoice: options?.toolChoice,
+        toolChoice: options?.toolChoice?.type === "tool"
+            ? { type: "function", function: { name: options.toolChoice.name } }
+            : options?.toolChoice,
+        parallelToolCalls: options?.parallelToolCalls,
     };
-    const clampedReasoning = options?.reasoning ? clampThinkingLevel(model, options.reasoning) : undefined;
-    const reasoning = clampedReasoning === "off" ? undefined : clampedReasoning;
-    const shouldUseReasoning = model.reasoning && reasoning !== undefined;
+    const reasoning = options?.reasoning === undefined || options.reasoning === "off"
+        ? options?.reasoning
+        : clampThinkingLevel(model, options.reasoning);
+    const shouldUseReasoning = model.reasoning && reasoning !== undefined && reasoning !== "off";
     return stream(model, context, {
         ...base,
         promptMode: shouldUseReasoning && usesPromptModeReasoning(model) ? "reasoning" : undefined,
-        reasoningEffort: shouldUseReasoning && usesReasoningEffort(model) ? mapReasoningEffort(model, reasoning) : undefined,
+        reasoningEffort: reasoning === "off" && usesReasoningEffort(model)
+            ? "none"
+            : shouldUseReasoning && usesReasoningEffort(model) ? mapReasoningEffort(model, reasoning) : undefined,
     });
 };
 function createOutput(model) {
@@ -368,6 +374,8 @@ function buildChatPayload(model, context, messages, options) {
         payload.maxTokens = options.maxTokens;
     if (options?.toolChoice)
         payload.toolChoice = mapToolChoice(options.toolChoice);
+    if (options?.parallelToolCalls !== undefined)
+        payload.parallelToolCalls = options.parallelToolCalls;
     if (options?.promptMode)
         payload.promptMode = options.promptMode;
     if (options?.reasoningEffort)
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.d.ts
index 704476d..edfd2e9 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.d.ts
@@ -5,7 +5,8 @@ export interface OpenAICodexResponsesOptions extends StreamOptions {
     reasoningSummary?: "auto" | "concise" | "detailed" | "off" | "on" | null;
     serviceTier?: ResponseCreateParamsStreaming["service_tier"];
     textVerbosity?: "low" | "medium" | "high";
-    toolChoice?: "auto" | "none" | "required";
+    toolChoice?: ResponseCreateParamsStreaming["tool_choice"];
+    parallelToolCalls?: boolean;
 }
 export declare const stream: StreamFunction<"openai-codex-responses", OpenAICodexResponsesOptions>;
 export declare const streamSimple: StreamFunction<"openai-codex-responses", SimpleStreamOptions>;
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.js b/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.js
index dfdb6ec..669d1ed 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/openai-codex-responses.js
@@ -351,6 +351,11 @@ export const stream = (model, context, options) => {
     })();
     return stream;
 };
+function mapSimpleToolChoice(choice) {
+    return choice?.type === "tool"
+        ? { type: "function", name: choice.name }
+        : choice;
+}
 export const streamSimple = (model, context, options) => {
     const apiKey = options?.apiKey;
     if (!apiKey) {
@@ -358,10 +363,14 @@ export const streamSimple = (model, context, options) => {
     }
     const base = {
         ...buildBaseOptions(model, context, options, apiKey),
-        toolChoice: options?.toolChoice,
+        toolChoice: mapSimpleToolChoice(options?.toolChoice),
+        parallelToolCalls: options?.parallelToolCalls,
     };
-    const clampedReasoning = options?.reasoning ? clampThinkingLevel(model, options.reasoning) : undefined;
-    const reasoningEffort = clampedReasoning === "off" ? undefined : clampedReasoning;
+    const reasoningEffort = options?.reasoning === undefined
+        ? undefined
+        : options.reasoning === "off"
+            ? "none"
+            : clampThinkingLevel(model, options.reasoning);
     return stream(model, context, {
         ...base,
         reasoningEffort,
@@ -396,7 +405,7 @@ function buildRequestBody(model, context, options, cacheSessionId, grammarToolIn
         include: ["reasoning.encrypted_content"],
         prompt_cache_key: cacheSessionId,
         tool_choice: options?.toolChoice ?? "auto",
-        parallel_tool_calls: true,
+        parallel_tool_calls: options?.parallelToolCalls ?? true,
     };
     if (options?.temperature !== undefined) {
         body.temperature = options.temperature;
@@ -424,9 +433,6 @@ function buildRequestBody(model, context, options, cacheSessionId, grammarToolIn
             };
         }
     }
-    else if (model.reasoning && model.thinkingLevelMap?.off !== null) {
-        body.reasoning = { effort: model.thinkingLevelMap?.off ?? "none" };
-    }
     return body;
 }
 function getServiceTierCostMultiplier(model, serviceTier) {
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/openai-completions.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/openai-completions.d.ts
index 7f14612..e09564c 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/openai-completions.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/openai-completions.d.ts
@@ -1,10 +1,11 @@
 import OpenAI from "openai";
 import type { ChatCompletionMessageParam } from "openai/resources/chat/completions.js";
-import type { Model, OpenAICompletionsCompat, SimpleStreamOptions, StreamFunction, StreamOptions, ThinkingBudgets } from "../types.ts";
+import type { Model, ModelThinkingLevel, OpenAICompletionsCompat, SimpleStreamOptions, StreamFunction, StreamOptions, ThinkingBudgets } from "../types.ts";
 import { type TranscriptContext } from "../utils/transcript.ts";
 export interface OpenAICompletionsOptions extends StreamOptions {
     toolChoice?: OpenAI.Chat.Completions.ChatCompletionToolChoiceOption;
-    reasoningEffort?: "minimal" | "low" | "medium" | "high" | "xhigh" | "max";
+    parallelToolCalls?: boolean;
+    reasoningEffort?: ModelThinkingLevel;
     /** Token budgets per thinking level. Used when `compat.thinkingTokenBudgetField` or `compat.supportsThinkingTokenBudget` is set, or by `{ "$var": "thinking.budget" }`. */
     thinkingBudgets?: ThinkingBudgets;
 }
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/openai-completions.js b/node_modules/@earendil-works/pi-ai/dist/api/openai-completions.js
index fd459fd..048ac09 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/openai-completions.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/openai-completions.js
@@ -514,14 +514,21 @@ export const stream = (model, context, options) => {
     })();
     return stream;
 };
+function mapSimpleToolChoice(choice) {
+    return choice?.type === "tool"
+        ? { type: "function", function: { name: choice.name } }
+        : choice;
+}
 export const streamSimple = (model, context, options) => {
     getClientApiKey(model.provider, options?.apiKey, options?.headers);
     const base = {
         ...buildBaseOptions(model, context, options, options?.apiKey),
-        toolChoice: options?.toolChoice,
+        toolChoice: mapSimpleToolChoice(options?.toolChoice),
+        parallelToolCalls: options?.parallelToolCalls,
     };
-    const clampedReasoning = options?.reasoning ? clampThinkingLevel(model, options.reasoning) : undefined;
-    const reasoningEffort = clampedReasoning === "off" ? undefined : clampedReasoning;
+    const reasoningEffort = options?.reasoning === undefined || options.reasoning === "off"
+        ? options?.reasoning
+        : clampThinkingLevel(model, options.reasoning);
     return stream(model, context, {
         ...base,
         reasoningEffort,
@@ -611,36 +618,48 @@ function buildParams(model, context, options, compat = getCompat(model), cacheRe
     if (options?.toolChoice) {
         params.tool_choice = options.toolChoice;
     }
+    if (options?.parallelToolCalls !== undefined) {
+        params.parallel_tool_calls = options.parallelToolCalls;
+    }
     if (compat.vllmPriority !== undefined) {
         params.priority = compat.vllmPriority;
     }
     const thinkingTokenBudgetField = resolveThinkingTokenBudgetField(compat);
     const thinkingBudget = resolveClampedThinkingBudget(model, options, params);
+    const requestedReasoningEffort = options?.reasoningEffort;
+    const reasoningEnabled = requestedReasoningEffort !== undefined && requestedReasoningEffort !== "off";
+    const reasoningDisabled = requestedReasoningEffort === "off";
     if (compat.thinkingFormat === "zai" && model.reasoning) {
         const zaiParams = params;
-        zaiParams.thinking = options?.reasoningEffort ? { type: "enabled", clear_thinking: false } : { type: "disabled" };
-        if (options?.reasoningEffort && compat.supportsReasoningEffort) {
-            const mappedEffort = model.thinkingLevelMap?.[options.reasoningEffort];
-            const effort = mappedEffort === undefined ? options.reasoningEffort : mappedEffort;
+        if (reasoningEnabled)
+            zaiParams.thinking = { type: "enabled", clear_thinking: false };
+        else if (reasoningDisabled)
+            zaiParams.thinking = { type: "disabled" };
+        if (reasoningEnabled && compat.supportsReasoningEffort) {
+            const mappedEffort = model.thinkingLevelMap?.[requestedReasoningEffort];
+            const effort = mappedEffort === undefined ? requestedReasoningEffort : mappedEffort;
             if (typeof effort === "string") {
                 zaiParams.reasoning_effort = effort;
             }
         }
     }
     else if (compat.thinkingFormat === "qwen" && model.reasoning) {
-        params.enable_thinking = !!options?.reasoningEffort;
-        if (options?.reasoningEffort && compat.supportsReasoningEffort) {
-            const effort = model.thinkingLevelMap?.[options.reasoningEffort] ?? options.reasoningEffort;
+        if (requestedReasoningEffort !== undefined)
+            params.enable_thinking = reasoningEnabled;
+        if (reasoningEnabled && compat.supportsReasoningEffort) {
+            const effort = model.thinkingLevelMap?.[requestedReasoningEffort] ?? requestedReasoningEffort;
             if (typeof effort === "string") {
                 params.reasoning_effort = effort;
             }
         }
     }
     else if (compat.thinkingFormat === "qwen-chat-template" && model.reasoning) {
-        params.chat_template_kwargs = {
-            enable_thinking: !!options?.reasoningEffort,
-            preserve_thinking: true,
-        };
+        if (requestedReasoningEffort !== undefined) {
+            params.chat_template_kwargs = {
+                enable_thinking: reasoningEnabled,
+                preserve_thinking: true,
+            };
+        }
     }
     else if (compat.thinkingFormat === "chat-template" && model.reasoning) {
         const chatTemplateKwargs = buildChatTemplateValues(model, options, compat.chatTemplateKwargs, thinkingBudget);
@@ -655,65 +674,67 @@ function buildParams(model, context, options, compat = getCompat(model), cacheRe
             basetenParams.chat_template_args = chatTemplateArgs;
         }
         if (compat.supportsReasoningEffort) {
-            const requestedEffort = options?.reasoningEffort;
-            const mappedEffort = requestedEffort ? model.thinkingLevelMap?.[requestedEffort] : model.thinkingLevelMap?.off;
-            const effort = mappedEffort === undefined ? requestedEffort : mappedEffort;
+            const mappedEffort = requestedReasoningEffort === undefined ? undefined : model.thinkingLevelMap?.[requestedReasoningEffort];
+            const effort = mappedEffort === undefined
+                ? (reasoningDisabled ? undefined : requestedReasoningEffort)
+                : mappedEffort;
             if (typeof effort === "string") {
                 basetenParams.reasoning_effort = effort;
             }
         }
     }
     else if (compat.thinkingFormat === "deepseek" && model.reasoning) {
-        if (options?.reasoningEffort) {
+        if (reasoningEnabled) {
             params.thinking = { type: "enabled" };
         }
-        else if (model.thinkingLevelMap?.off !== null) {
+        else if (reasoningDisabled && model.thinkingLevelMap?.off !== null) {
             params.thinking = { type: "disabled" };
         }
-        if (options?.reasoningEffort && compat.supportsReasoningEffort) {
+        if (reasoningEnabled && compat.supportsReasoningEffort) {
             params.reasoning_effort =
-                model.thinkingLevelMap?.[options.reasoningEffort] ?? options.reasoningEffort;
+                model.thinkingLevelMap?.[requestedReasoningEffort] ?? requestedReasoningEffort;
         }
     }
     else if (compat.thinkingFormat === "openrouter" && model.reasoning) {
         // OpenRouter normalizes reasoning across providers via a nested reasoning object.
         const openRouterParams = params;
-        if (options?.reasoningEffort) {
+        if (reasoningEnabled) {
             openRouterParams.reasoning = {
-                effort: model.thinkingLevelMap?.[options.reasoningEffort] ?? options.reasoningEffort,
+                effort: model.thinkingLevelMap?.[requestedReasoningEffort] ?? requestedReasoningEffort,
             };
         }
-        else if (model.thinkingLevelMap?.off !== null) {
+        else if (reasoningDisabled && model.thinkingLevelMap?.off !== null) {
             openRouterParams.reasoning = { effort: model.thinkingLevelMap?.off ?? "none" };
         }
     }
-    else if (compat.thinkingFormat === "ant-ling" && model.reasoning && options?.reasoningEffort) {
-        const effort = model.thinkingLevelMap?.[options.reasoningEffort];
+    else if (compat.thinkingFormat === "ant-ling" && model.reasoning && reasoningEnabled) {
+        const effort = model.thinkingLevelMap?.[requestedReasoningEffort];
         if (typeof effort === "string") {
             params.reasoning = { effort };
         }
     }
     else if (compat.thinkingFormat === "together" && model.reasoning) {
         const togetherParams = params;
-        togetherParams.reasoning = { enabled: !!options?.reasoningEffort };
-        if (options?.reasoningEffort && compat.supportsReasoningEffort) {
-            togetherParams.reasoning_effort = model.thinkingLevelMap?.[options.reasoningEffort] ?? options.reasoningEffort;
+        if (requestedReasoningEffort !== undefined)
+            togetherParams.reasoning = { enabled: reasoningEnabled };
+        if (reasoningEnabled && compat.supportsReasoningEffort) {
+            togetherParams.reasoning_effort = model.thinkingLevelMap?.[requestedReasoningEffort] ?? requestedReasoningEffort;
         }
     }
     else if (compat.thinkingFormat === "string-thinking" && model.reasoning) {
         const stringThinkingParams = params;
-        if (options?.reasoningEffort) {
-            stringThinkingParams.thinking = model.thinkingLevelMap?.[options.reasoningEffort] ?? options.reasoningEffort;
+        if (reasoningEnabled) {
+            stringThinkingParams.thinking = model.thinkingLevelMap?.[requestedReasoningEffort] ?? requestedReasoningEffort;
         }
-        else if (model.thinkingLevelMap?.off !== null) {
+        else if (reasoningDisabled && model.thinkingLevelMap?.off !== null) {
             stringThinkingParams.thinking = model.thinkingLevelMap?.off ?? "none";
         }
     }
-    else if (options?.reasoningEffort && model.reasoning && compat.supportsReasoningEffort) {
+    else if (reasoningEnabled && model.reasoning && compat.supportsReasoningEffort) {
         // OpenAI-style reasoning_effort
-        params.reasoning_effort = model.thinkingLevelMap?.[options.reasoningEffort] ?? options.reasoningEffort;
+        params.reasoning_effort = model.thinkingLevelMap?.[requestedReasoningEffort] ?? requestedReasoningEffort;
     }
-    else if (!options?.reasoningEffort && model.reasoning && compat.supportsReasoningEffort) {
+    else if (reasoningDisabled && model.reasoning && compat.supportsReasoningEffort) {
         const offValue = model.thinkingLevelMap?.off;
         if (typeof offValue === "string") {
             params.reasoning_effort = offValue;
@@ -756,7 +777,7 @@ function resolveThinkingTokenBudgetField(compat) {
     return undefined;
 }
 function resolveClampedThinkingBudget(model, options, params) {
-    if (!options?.reasoningEffort || !model.reasoning)
+    if (options?.reasoningEffort === undefined || options.reasoningEffort === "off" || !model.reasoning)
         return undefined;
     const ceiling = params.max_tokens ?? params.max_completion_tokens ?? model.maxTokens;
     const budget = clampThinkingBudgetToAnswerRoom(thinkingBudgetForLevel(options.reasoningEffort, options.thinkingBudgets), ceiling);
@@ -777,17 +798,22 @@ function resolveChatTemplateKwargValue(model, options, value, thinkingBudget) {
         return value;
     }
     const reasoningEffort = options?.reasoningEffort;
-    if (!reasoningEffort && value.omitWhenOff) {
+    if (reasoningEffort === undefined) {
+        return undefined;
+    }
+    if (reasoningEffort === "off" && value.omitWhenOff) {
         return undefined;
     }
     if (value.$var === "thinking.enabled") {
-        return !!reasoningEffort;
+        return reasoningEffort !== "off";
     }
     if (value.$var === "thinking.budget") {
         return thinkingBudget;
     }
-    const mappedValue = reasoningEffort ? model.thinkingLevelMap?.[reasoningEffort] : model.thinkingLevelMap?.off;
-    return mappedValue === undefined ? reasoningEffort : typeof mappedValue === "string" ? mappedValue : undefined;
+    const mappedValue = model.thinkingLevelMap?.[reasoningEffort];
+    return mappedValue === undefined
+        ? (reasoningEffort === "off" ? undefined : reasoningEffort)
+        : typeof mappedValue === "string" ? mappedValue : undefined;
 }
 function getCompatCacheControl(compat, cacheRetention) {
     if (compat.cacheControlFormat !== "anthropic" || cacheRetention === "none") {
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.d.ts
index 41267bd..9a8bbfa 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.d.ts
@@ -1,10 +1,11 @@
 import type { ResponseCreateParamsStreaming } from "openai/resources/responses/responses.js";
-import type { SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
+import type { ModelThinkingLevel, SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
 export interface OpenAIResponsesOptions extends StreamOptions {
-    reasoningEffort?: "minimal" | "low" | "medium" | "high" | "xhigh" | "max";
+    reasoningEffort?: ModelThinkingLevel;
     reasoningSummary?: "auto" | "detailed" | "concise" | null;
     serviceTier?: ResponseCreateParamsStreaming["service_tier"];
     toolChoice?: ResponseCreateParamsStreaming["tool_choice"];
+    parallelToolCalls?: boolean;
 }
 /**
  * Generate function for OpenAI Responses API
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js b/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js
index 1396c8d..e89987a 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/openai-responses.js
@@ -158,14 +158,21 @@ export const stream = (model, context, options) => {
     })();
     return stream;
 };
+function mapSimpleToolChoice(choice) {
+    return choice?.type === "tool"
+        ? { type: "function", name: choice.name }
+        : choice;
+}
 export const streamSimple = (model, context, options) => {
     getClientApiKey(model.provider, options?.apiKey, options?.headers);
     const base = {
         ...buildBaseOptions(model, context, options, options?.apiKey),
-        toolChoice: options?.toolChoice,
+        toolChoice: mapSimpleToolChoice(options?.toolChoice),
+        parallelToolCalls: options?.parallelToolCalls,
     };
-    const clampedReasoning = options?.reasoning ? clampThinkingLevel(model, options.reasoning) : undefined;
-    const reasoningEffort = clampedReasoning === "off" ? undefined : clampedReasoning;
+    const reasoningEffort = options?.reasoning === undefined || options.reasoning === "off"
+        ? options?.reasoning
+        : clampThinkingLevel(model, options.reasoning);
     return stream(model, context, {
         ...base,
         reasoningEffort,
@@ -228,7 +235,10 @@ function buildParams(model, context, options, compat = getCompat(model), grammar
         store: false,
     };
     if (options?.maxTokens && compat.supportsMaxOutputTokens) {
-        params.max_output_tokens = Math.max(options.maxTokens, OPENAI_RESPONSES_MIN_OUTPUT_TOKENS);
+        if (options.maxTokens < OPENAI_RESPONSES_MIN_OUTPUT_TOKENS) {
+            throw new Error(`OpenAI Responses requires maxTokens >= ${OPENAI_RESPONSES_MIN_OUTPUT_TOKENS}`);
+        }
+        params.max_output_tokens = options.maxTokens;
     }
     if (options?.temperature !== undefined) {
         params.temperature = options?.temperature;
@@ -245,21 +255,21 @@ function buildParams(model, context, options, compat = getCompat(model), grammar
     if (options?.toolChoice !== undefined) {
         params.tool_choice = options.toolChoice;
     }
+    if (options?.parallelToolCalls !== undefined) {
+        params.parallel_tool_calls = options.parallelToolCalls;
+    }
     if (model.reasoning) {
-        if (options?.reasoningEffort || options?.reasoningSummary) {
-            const effort = options?.reasoningEffort
-                ? (model.thinkingLevelMap?.[options.reasoningEffort] ?? options.reasoningEffort)
+        if (options?.reasoningEffort !== undefined || options?.reasoningSummary) {
+            const effort = options?.reasoningEffort !== undefined
+                ? (model.thinkingLevelMap?.[options.reasoningEffort] ?? (options.reasoningEffort === "off" ? "none" : options.reasoningEffort))
                 : "medium";
-            params.reasoning = {
-                effort: effort,
-                summary: options?.reasoningSummary || "auto",
-            };
-            params.include = ["reasoning.encrypted_content"];
-        }
-        else if (model.provider !== "github-copilot" && model.thinkingLevelMap?.off !== null) {
-            params.reasoning = {
-                effort: (model.thinkingLevelMap?.off ?? "none"),
-            };
+            if (effort !== null) {
+                params.reasoning = {
+                    effort: effort,
+                    summary: options?.reasoningSummary || "auto",
+                };
+                params.include = ["reasoning.encrypted_content"];
+            }
         }
         if (model.provider === "xai")
             params.include = ["reasoning.encrypted_content"];
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/pi-messages.d.ts b/node_modules/@earendil-works/pi-ai/dist/api/pi-messages.d.ts
index 8817c76..266c848 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/pi-messages.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/api/pi-messages.d.ts
@@ -11,12 +11,8 @@
 import type { AssistantMessage, JsonObject, SimpleStreamOptions, StreamFunction, StreamOptions, ThinkingLevel, ToolCall } from "../types.ts";
 export interface PiMessagesOptions extends StreamOptions {
     reasoning?: ThinkingLevel;
-    toolChoice?: "auto" | "none" | "required" | {
-        type: "function";
-        function: {
-            name: string;
-        };
-    };
+    toolChoice?: import("../types.ts").ToolChoice;
+    parallelToolCalls?: boolean;
     /** Ask the backend for debug metadata (e.g. routing response headers). */
     debug?: boolean;
 }
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/pi-messages.js b/node_modules/@earendil-works/pi-ai/dist/api/pi-messages.js
index ad5f460..17cafaf 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/pi-messages.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/pi-messages.js
@@ -261,6 +261,7 @@ export const stream = (model, context, options) => {
                     cacheRetention: resolveCacheRetention(options?.cacheRetention, options?.env),
                     sessionId: options?.sessionId,
                     toolChoice: options?.toolChoice,
+                    parallelToolCalls: options?.parallelToolCalls,
                 },
             };
             const nextPayload = await options?.onPayload?.(payload, model);
@@ -307,6 +308,7 @@ export const streamSimple = (model, context, options) => {
         ...options,
         reasoning: options?.reasoning,
         toolChoice: options?.toolChoice,
+        parallelToolCalls: options?.parallelToolCalls,
         debug: extra?.debug,
     });
 };
diff --git a/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js b/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js
index 216a45f..0c59125 100644
--- a/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js
+++ b/node_modules/@earendil-works/pi-ai/dist/api/simple-options.js
@@ -57,7 +57,7 @@ export function adjustMaxTokensForThinking(
 // Undefined means no explicit caller cap. Use the model cap and fit thinking inside it.
 baseMaxTokens, modelMaxTokens, reasoningLevel, customBudgets) {
     let thinkingBudget = thinkingBudgetForLevel(reasoningLevel, customBudgets);
-    const maxTokens = baseMaxTokens === undefined ? modelMaxTokens : Math.min(baseMaxTokens + thinkingBudget, modelMaxTokens);
+    const maxTokens = baseMaxTokens === undefined ? modelMaxTokens : Math.min(baseMaxTokens, modelMaxTokens);
     if (maxTokens <= thinkingBudget) {
         thinkingBudget = clampThinkingBudgetToAnswerRoom(thinkingBudget, maxTokens);
     }
diff --git a/node_modules/@earendil-works/pi-ai/dist/types.d.ts b/node_modules/@earendil-works/pi-ai/dist/types.d.ts
index 49f2a32..51cc5e3 100644
--- a/node_modules/@earendil-works/pi-ai/dist/types.d.ts
+++ b/node_modules/@earendil-works/pi-ai/dist/types.d.ts
@@ -20,7 +20,10 @@ export type KnownProvider = "amazon-bedrock" | "ant-ling" | "anthropic" | "googl
 export type ProviderId = KnownProvider | string;
 export type KnownImagesProvider = "openrouter";
 export type ImagesProviderId = KnownImagesProvider | string;
-export type ToolChoice = "auto" | "none";
+export type ToolChoice = "auto" | "none" | "required" | {
+    type: "tool";
+    name: string;
+};
 export type ThinkingLevel = "minimal" | "low" | "medium" | "high" | "xhigh" | "max";
 export type ModelThinkingLevel = "off" | ThinkingLevel;
 export type ThinkingLevelMap = Partial<Record<ModelThinkingLevel, string | null>>;
@@ -224,7 +227,10 @@ export interface AnthropicAllowedFallbackModel {
 export interface SimpleStreamOptions extends StreamOptions {
     /** Provider-neutral tool selection for simple requests. When omitted, adapters use provider-specific behavior. */
     toolChoice?: ToolChoice;
-    reasoning?: ThinkingLevel;
+    /** Whether the Provider may emit more than one tool call in a turn. Unsupported Providers ignore this preference. */
+    parallelToolCalls?: boolean;
+    /** Omitted preserves the Provider default; "off" explicitly disables reasoning. */
+    reasoning?: ModelThinkingLevel;
     /** Ask a capable provider to return a durable handle and continue the request asynchronously. */
     deferred?: boolean | {
         window?: "15m" | "1h" | "24h";
